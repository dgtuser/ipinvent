\"notes\"],\n        \"createdAt\": row[\"created_at\"],\n        \"updatedAt\": row[\"updated_at\"],\n    }\n\n\ndef row_to_activity(row: sqlite3.Row) -> dict:\n    return {\n        \"id\": row[\"id\"],\n        \"action\": row[\"action\"],\n        \"entityType\": row[\"entity_type\"],\n        \"entityLabel\": row[\"entity_label\"],\n        \"details\": row[\"details\"],\n        \"createdAt\": row[\"created_at\"],\n        \"actor\": row[\"display_name\"] or row[\"username\"] or \"System\",\n    }\n\n\ndef init_db() -> None:\n    with get_connection() as conn:\n        conn.executescript(\n            \"\"\"\n            CREATE TABLE IF NOT EXISTS users (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                username TEXT NOT NULL UNIQUE,\n                display_name TEXT NOT NULL,\n                password_salt TEXT NOT NULL,\n                password_hash TEXT NOT NULL,\n                role TEXT NOT NULL,\n                is_active INTEGER NOT NULL DEFAULT 1,\n                created_at TEXT NOT NULL,\n                updated_at TEXT NOT NULL,\n                last_login_at TEXT\n            );\n\n            CREATE TABLE IF NOT EXISTS sessions (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                user_id INTEGER NOT NULL,\n                token_hash TEXT NOT NULL UNIQUE,\n                expires_at TEXT NOT NULL,\n                created_at TEXT NOT NULL,\n                FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE\n            );\n\n            CREATE TABLE IF NOT EXISTS groups (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                name TEXT NOT NULL UNIQUE,\n                description TEXT NOT NULL DEFAULT '',\n                created_at TEXT NOT NULL,\n                updated_at TEXT NOT NULL\n            );\n\n            CREATE TABLE IF NOT EXISTS records (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                ip_address TEXT NOT NULL,\n                ip_key TEXT NOT NULL UNIQUE,\n                hostname TEXT NOT NULL DEFAULT '',\n                group_id INTEGER,\n                status TEXT NOT NULL DEFAULT 'Active',\n                device_type TEXT NOT NULL DEFAULT '',\n                environment TEXT NOT NULL DEFAULT 'Production',\n                subnet TEXT NOT NULL DEFAULT '',\n                vlan TEXT NOT NULL DEFAULT '',\n                gateway TEXT NOT NULL DEFAULT '',\n                mac_address TEXT NOT NULL DEFAULT '',\n                dns_name TEXT NOT NULL DEFAULT '',\n                location TEXT NOT NULL DEFAULT '',\n                owner TEXT NOT NULL DEFAULT '',\n                assigned_at TEXT NOT NULL DEFAULT '',\n                last_audit TEXT NOT NULL DEFAULT '',\n                tags TEXT NOT NULL DEFAULT '',\n                description TEXT NOT NULL DEFAULT '',\n                notes TEXT NOT NULL DEFAULT '',\n                created_by INTEGER,\n                updated_by INTEGER,\n                created_at TEXT NOT NULL,\n                updated_at TEXT NOT NULL,\n                FOREIGN KEY (group_id) REFERENCES groups(id) ON DELETE SET NULL,\n                FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE SET NULL,\n                FOREIGN KEY (updated_by) REFERENCES users(id) ON DELETE SET NULL\n            );\n\n            CREATE TABLE IF NOT EXISTS activity_logs (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                user_id INTEGER,\n                action TEXT NOT NULL,\n                entity_type TEXT NOT NULL,\n                entity_label TEXT NOT NULL,\n                details TEXT NOT NULL DEFAULT '',\n                created_at TEXT NOT NULL,\n                FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL\n            );\n            \"\"\"\n        )\n\n        timestamp = now_iso()\n        for name, description in DEFAULT_GROUPS:\n            conn.execute(\n                \"\"\"\n                INSERT OR IGNORE INTO groups (name, description, created_at, updated_at)\n                VALUES (?, ?, ?, ?)\n                \"\"\",\n                (name, description, timestamp, timestamp),\n            )\n\n        conn.execute(\"DELETE FROM sessions WHERE expires_at <= ?\", (timestamp,))\n\n\ndef seed_sample_records(conn: sqlite3.Connection, actor_id: int) -> None:\n    count = conn.execute(\"SELECT COUNT(*) AS total FROM records\").fetchone()[\"total\"]\n    if count:\n        return\n\n    group_map = {\n        row[\"name\"]: row[\"id\"] for row in conn.execute(\"SELECT id, name FROM groups\").fetchall()\n    }\n    timestamp = now_iso()\n\n    for record in SAMPLE_RECORDS:\n        conn.execute(\n            \"\"\"\n            INSERT INTO records (\n                ip_address, ip_key, hostname, group_id, status, device_type, environment,\n                subnet, vlan, gateway, mac_address, dns_name, location, owner,\n                assigned_at, last_audit, tags, description, notes,\n                created_by, updated_by, created_at, updated_at\n            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)\n            \"\"\",\n            (\n                record[\"ip_address\"],\n                record[\"ip_address\"].casefold(),\n                record[\"hostname\"],\n                group_map.get(record[\"group_name\"]),\n                record[\"status\"],\n                record[\"device_type\"],\n                record[\"environment\"],\n                record[\"subnet\"],\n                record[\"vlan\"],\n                record[\"gateway\"],\n                record[\"mac_address\"],\n                record[\"dns_name\"],\n                record[\"location\"],\n                record[\"owner\"],\n                record[\"assigned_at\"],\n                record[\"last_audit\"],\n                record[\"tags\"],\n                record[\"description\"],\n                record[\"notes\"],\n                actor_id,\n                actor_id,\n                timestamp,\n                timestamp,\n            ),\n        )\n\n\ndef log_activity(\n    conn: sqlite3.Connection,\n    user_id: int | None,\n    action: str,\n    entity_type: str,\n    entity_label: str,\n    details: str = \"\",\n) -> None:\n    conn.execute(\n        \"\"\"\n        INSERT INTO activity_logs (user_id, action, entity_type, entity_label, details, created_at)\n        VALUES (?, ?, ?, ?, ?, ?)\n        \"\"\",\n        (user_id, action, entity_type, entity_label, details, now_iso()),\n    )\n\n\ndef get_group_id(\n    conn: sqlite3.Connection,\n    group_id: object = None,\n    group_name: object = None,\n) -> int | None:\n    if group_id not in (None, \"\"):\n        row = conn.execute(\"SELECT id FROM groups WHERE id = ?\", (group_id,)).fetchone()\n        if row is None:\n            raise ValueError(\"\u0412\u044b\u0431\u0440\u0430\u043d\u043d\u0430\u044f \u0433\u0440\u0443\u043f\u043f\u0430 \u043d\u0435 \u043d\u0430\u0439\u0434\u0435\u043d\u0430\")\n        return row[\"id\"]\n\n    name = normalize_text(group_name)\n    if not name:\n        return None\n\n    existing = conn.execute(\"SELECT id FROM groups WHERE lower(name) = lower(?)\", (name,)).fetchone()\n    if existing is not None:\n        return existing[\"id\"]\n\n    timestamp = now_iso()\n    cursor = conn.execute(\n        \"\"\"\n        INSERT INTO groups (name, description, created_at, updated_at)\n        VALUES (?, '', ?, ?)\n        \"\"\",\n        (name, timestamp, timestamp),\n    )\n    return cursor.lastrowid\n\n\ndef parse_record_payload(conn: sqlite3.Connection, payload: dict) -> dict:\n    ip_address = normalize_text(payload.get(\"ipAddress\"))\n    gateway = normalize_text(payload.get(\"gateway\"))\n    mac_address = normalize_text(payload.get(\"macAddress\")).replace(\"-\", \":\").upper()\n    assigned_at = normalize_text(payload.get(\"assignedAt\"))\n    last_audit = normalize_text(payload.get(\"lastAudit\"))\n\n    if not validate_ip(ip_addr