hash, role,\n                    is_active, created_at, updated_at\n                ) VALUES (?, ?, ?, ?, ?, 1, ?, ?)\n                \"\"\",\n                (username, display_name, salt, digest, role, timestamp, timestamp),\n            )\n        except sqlite3.IntegrityError as error:\n            raise ApiError(\"\u041f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u044c \u0441 \u0442\u0430\u043a\u0438\u043c \u043b\u043e\u0433\u0438\u043d\u043e\u043c \u0443\u0436\u0435 \u0441\u0443\u0449\u0435\u0441\u0442\u0432\u0443\u0435\u0442\", 409) from error\n\n        user = conn.execute(\"SELECT * FROM users WHERE id = ?\", (cursor.lastrowid,)).fetchone()\n        log_activity(conn, actor[\"id\"], \"create\", \"user\", username, f\"\u0421\u043e\u0437\u0434\u0430\u043d \u043f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u044c \u0441 \u0440\u043e\u043b\u044c\u044e {role}\")\n        self.send_json(201, {\"user\": row_to_user(user)})\n\n    def api_update_user(self, conn: sqlite3.Connection, user_id: int) -> None:\n        actor = self.require_user(conn, (\"admin\",))\n        target = conn.execute(\"SELECT * FROM users WHERE id = ?\", (user_id,)).fetchone()\n        if target is None:\n            raise ApiError(\"\u041f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u044c \u043d\u0435 \u043d\u0430\u0439\u0434\u0435\u043d\", 404)\n\n        payload = self.request_json()\n        role = normalize_text(payload.get(\"role\")) or target[\"role\"]\n        display_name = normalize_text(payload.get(\"displayName\")) or target[\"display_name\"]\n        password = normalize_text(payload.get(\"password\"))\n        is_active = payload.get(\"isActive\")\n        if role not in {\"admin\", \"operator\", \"viewer\"}:\n            raise ApiError(\"\u041d\u0435\u0434\u043e\u043f\u0443\u0441\u0442\u0438\u043c\u0430\u044f \u0440\u043e\u043b\u044c\")\n        if is_active is None:\n            is_active = bool(target[\"is_active\"])\n        else:\n            is_active = bool(is_active)\n\n        removing_admin = target[\"role\"] == \"admin\" and (role != \"admin\" or not is_active)\n        if removing_admin and active_admin_count(conn) <= 1:\n            raise ApiError(\"\u0412 \u0441\u0438\u0441\u0442\u0435\u043c\u0435 \u0434\u043e\u043b\u0436\u0435\u043d \u043e\u0441\u0442\u0430\u0442\u044c\u0441\u044f \u0445\u043e\u0442\u044f \u0431\u044b \u043e\u0434\u0438\u043d \u0430\u043a\u0442\u0438\u0432\u043d\u044b\u0439 \u0430\u0434\u043c\u0438\u043d\u0438\u0441\u0442\u0440\u0430\u0442\u043e\u0440\", 409)\n        if actor[\"id\"] == target[\"id\"] and not is_active:\n            raise ApiError(\"\u041d\u0435\u043b\u044c\u0437\u044f \u0434\u0435\u0430\u043a\u0442\u0438\u0432\u0438\u0440\u043e\u0432\u0430\u0442\u044c \u0442\u0435\u043a\u0443\u0449\u0435\u0433\u043e \u043f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u044f\", 409)\n\n        timestamp = now_iso()\n        if password:\n            if len(password) < 8:\n                raise ApiError(\"\u041f\u0430\u0440\u043e\u043b\u044c \u0434\u043e\u043b\u0436\u0435\u043d \u0431\u044b\u0442\u044c \u043d\u0435 \u043a\u043e\u0440\u043e\u0447\u0435 8 \u0441\u0438\u043c\u0432\u043e\u043b\u043e\u0432\")\n            salt, digest = password_hash(password)\n            conn.execute(\n                \"\"\"\n                UPDATE users\n                SET display_name = ?, role = ?, is_active = ?, password_salt = ?,\n                    password_hash = ?, updated_at = ?\n                WHERE id = ?\n                \"\"\",\n                (display_name, role, int(is_active), salt, digest, timestamp, user_id),\n            )\n        else:\n            conn.execute(\n                \"\"\"\n                UPDATE users\n                SET display_name = ?, role = ?, is_active = ?, updated_at = ?\n                WHERE id = ?\n                \"\"\",\n                (display_name, role, int(is_active), timestamp, user_id),\n            )\n\n        user = conn.execute(\"SELECT * FROM users WHERE id = ?\", (user_id,)).fetchone()\n        log_activity(conn, actor[\"id\"], \"update\", \"user\", user[\"username\"], \"\u041e\u0431\u043d\u043e\u0432\u043b\u0435\u043d\u044b \u043f\u0430\u0440\u0430\u043c\u0435\u0442\u0440\u044b \u043f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u044f\")\n        self.send_json(200, {\"user\": row_to_user(user)})\n\n    def api_list_activity(self, conn: sqlite3.Connection) -> None:\n        self.require_user(conn)\n        rows = conn.execute(\n            \"\"\"\n            SELECT a.*, u.username, u.display_name\n            FROM activity_logs a\n            LEFT JOIN users u ON u.id = a.user_id\n            ORDER BY a.created_at DESC\n            LIMIT 30\n            \"\"\"\n        ).fetchall()\n        self.send_json(200, {\"activity\": [row_to_activity(row) for row in rows]})\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=\"IP inventory server\")\n    parser.add_argument(\"--host\", default=\"127.0.0.1\")\n    parser.add_argument(\"--port\", type=int, default=8000)\n    args = parser.parse_args()\n\n    init_db()\n    server = ThreadingHTTPServer((args.host, args.port), AppHandler)\n    print(f\"IP Invent running on http://{args.host}:{args.port}\")\n    try:\n        server.serve_forever()\n    except KeyboardInterrupt:\n        pass\n    finally:\n        server.server_close()\n\n\nif __name__ == \"__main__\":\n    main()\n