) {\n  const records = getFilteredRecords();\n  if (!records.length) {\n    showToast(\"\u041d\u0435\u0442 \u0434\u0430\u043d\u043d\u044b\u0445 \u0434\u043b\u044f \u044d\u043a\u0441\u043f\u043e\u0440\u0442\u0430\", \"error\");\n    return;\n  }\n\n  const delimiter = \";\";\n  const header = CSV_HEADERS.map(([, label]) => escapeCsvValue(label, delimiter)).join(delimiter);\n  const lines = records.map((record) =>\n    CSV_HEADERS.map(([field]) => escapeCsvValue(record[field] || \"\", delimiter)).join(delimiter),\n  );\n  const content = [header].concat(lines).join(\"\\r\\n\");\n\n  const blob = new Blob([content], { type: \"text/csv;charset=utf-8;\" });\n  const url = URL.createObjectURL(blob);\n  const link = document.createElement(\"a\");\n  link.href = url;\n  link.download = \"ip-inventory-export.csv\";\n  link.click();\n  URL.revokeObjectURL(url);\n\n  showToast(\"CSV \u044d\u043a\u0441\u043f\u043e\u0440\u0442\u0438\u0440\u043e\u0432\u0430\u043d\", \"success\");\n}\n\nfunction detectDelimiter(text) {\n  const firstLine = text.replace(/^\\uFEFF/, \"\").split(/\\r?\\n/, 1)[0] || \"\";\n  const semicolons = countUnquotedDelimiter(firstLine, \";\");\n  const commas = countUnquotedDelimiter(firstLine, \",\");\n  return semicolons > commas ? \";\" : \",\";\n}\n\nfunction countUnquotedDelimiter(line, delimiter) {\n  let inQuotes = false;\n  let total = 0;\n\n  for (let index = 0; index < line.length; index += 1) {\n    const char = line[index];\n    const next = line[index + 1];\n    if (char === '\"') {\n      if (inQuotes && next === '\"') {\n        index += 1;\n      } else {\n        inQuotes = !inQuotes;\n      }\n      continue;\n    }\n    if (char === delimiter && !inQuotes) {\n      total += 1;\n    }\n  }\n\n  return total;\n}\n\nfunction parseCsv(text, delimiter) {\n  const rows = [];\n  let row = [];\n  let cell = \"\";\n  let inQuotes = false;\n  const normalized = text.replace(/^\\uFEFF/, \"\");\n\n  for (let index = 0; index < normalized.length; index += 1) {\n    const char = normalized[index];\n    const next = normalized[index + 1];\n\n    if (char === '\"') {\n      if (inQuotes && next === '\"') {\n        cell += '\"';\n        index += 1;\n      } else {\n        inQuotes = !inQuotes;\n      }\n      continue;\n    }\n\n    if (char === delimiter && !inQuotes) {\n      row.push(cell.trim());\n      cell = \"\";\n      continue;\n    }\n\n    if ((char === \"\\n\" || char === \"\\r\") && !inQuotes) {\n      if (char === \"\\r\" && next === \"\\n\") {\n        index += 1;\n      }\n      row.push(cell.trim());\n      if (row.some((value) => value !== \"\")) {\n        rows.push(row);\n      }\n      row = [];\n      cell = \"\";\n      continue;\n    }\n\n    cell += char;\n  }\n\n  row.push(cell.trim());\n  if (row.some((value) => value !== \"\")) {\n    rows.push(row);\n  }\n\n  return rows;\n}\n\nfunction resolveCsvField(header) {\n  const normalized = normalizeHeader(header);\n  return CSV_ALIASES[normalized] || SEARCH_FIELDS.find((field) => normalizeHeader(field) === normalized) || \"\";\n}\n\nfunction normalizeHeader(value) {\n  return String(value || \"\")\n    .trim()\n    .toLowerCase()\n    .replace(/[_/]+/g, \" \")\n    .replace(/\\s+/g, \" \" );\n}\n\nfunction escapeCsvValue(value, delimiter) {\n  const text = String(value ?? \"\");\n  if (text.includes('\"') || text.includes(\"\\n\") || text.includes(\"\\r\") || text.includes(delimiter)) {\n    return `\"${text.replace(/\"/g, '\"\"')}\"`;\n  }\n  return text;\n}\n\nfunction renderStatusBadge(status) {\n  const normalized = String(status || \"Active\").toLowerCase();\n  let className = \"status-active\";\n\n  if (normalized === \"reserved\") {\n    className = \"status-reserved\";\n  } else if (normalized === \"maintenance\") {\n    className = \"status-maintenance\";\n  } else if (normalized === \"offline\") {\n    className = \"status-offline\";\n  }\n\n  return `<span class=\"status-badge ${className}\">${escapeHtml(status || \"Active\")}</span>`;\n}\n\nfunction renderAuditBadge(value) {\n  if (needsAudit(value)) {\n    return '<span class=\"audit-badge warning\">\u041d\u0443\u0436\u0435\u043d \u0430\u0443\u0434\u0438\u0442</span>';\n  }\n  return `<span class=\"audit-badge ok\">${escapeHtml(formatDate(value) || \"\u0410\u043a\u0442\u0443\u0430\u043b\u044c\u043d\u043e\")}</span>`;\n}\n\nfunction needsAudit(value) {\n  if (!value) {\n    return true;\n  }\n\n  const date = new Date(value);\n  if (Number.isNaN(date.getTime())) {\n    return true;\n  }\n\n  const threshold = new Date();\n  threshold.setDate(threshold.getDate() - AUDIT_WARNING_DAYS);\n  return date < threshold;\n}\n\nfunction compareIp(left, right) {\n  const leftParts = parseIpv4(left);\n  const rightParts = parseIpv4(right);\n  if (leftParts && rightParts) {\n    for (let index = 0; index < 4; index += 1) {\n      if (leftParts[index] !== rightParts[index]) {\n        return leftParts[index] - rightParts[index];\n      }\n    }\n    return 0;\n  }\n  return String(left || \"\").localeCompare(String(right || \"\"), \"en\", { sensitivity: \"base\" });\n}\n\nfunction parseIpv4(value) {\n  if (!/^\\d{1,3}(\\.\\d{1,3}){3}$/.test(String(value || \"\"))) {\n    return null;\n  }\n  return value.split(\".\").map(Number);\n}\n\nfunction activityLabel(action, entityType) {\n  const left = {\n    create: \"\u0421\u043e\u0437\u0434\u0430\u043d\u0438\u0435\",\n    update: \"\u041e\u0431\u043d\u043e\u0432\u043b\u0435\u043d\u0438\u0435\",\n    delete: \"\u0423\u0434\u0430\u043b\u0435\u043d\u0438\u0435\",\n    import: \"\u0418\u043c\u043f\u043e\u0440\u0442\",\n  }[action] || \"\u0414\u0435\u0439\u0441\u0442\u0432\u0438\u0435\";\n\n  const right = {\n    record: \"\u0437\u0430\u043f\u0438\u0441\u0438\",\n    group: \"\u0433\u0440\u0443\u043f\u043f\u044b\",\n    user: \"\u043f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u044f\",\n  }[entityType] || \"\u043e\u0431\u044a\u0435\u043a\u0442\u0430\";\n\n  return `${left} ${right}`;\n}\n\nfunction roleLabel(role) {\n  return {\n    admin: \"Admin\",\n    operator: \"Operator\",\n    viewer: \"Viewer\",\n  }[role] || role;\n}\n\nfunction isAdmin() {\n  return state.user?.role === \"admin\";\n}\n\nfunction canEditInventory() {\n  return state.user?.role === \"admin\" || state.user?.role === \"operator\";\n}\n\nfunction uniqueValues(values) {\n  return Array.from(new Set(values)).sort((left, right) => left.localeCompare(right, \"ru\"));\n}\n\nfunction formatDate(value) {\n  if (!value) {\n    return \"\";\n  }\n  const date = new Date(value);\n  if (Number.isNaN(date.getTime())) {\n    return value;\n  }\n  return new Intl.DateTimeFormat(\"ru-RU\").format(date);\n}\n\nfunction formatDateTime(value) {\n  if (!value) {\n    return \"\";\n  }\n  const date = new Date(value);\n  if (Number.isNaN(date.getTime())) {\n    return value;\n  }\n  return new Intl.DateTimeFormat(\"ru-RU\", {\n    dateStyle: \"short\",\n    timeStyle: \"short\",\n  }).format(date);\n}\n\nfunction showToast(message, kind = \"default\") {\n  const toast = document.createElement(\"div\");\n  toast.className = `toast ${kind}`;\n  toast.textContent = message;\n  elements.toastContainer.appendChild(toast);\n\n  window.setTimeout(() => {\n    toast.remove();\n  }, 3600);\n}\n\nfunction escapeHtml(value) {\n  return String(value ?? \"\")\n    .replaceAll(\"&\", \"&amp;\")\n    .replaceAll(\"<\", \"&lt;\")\n    .replaceAll(\">\", \"&gt;\")\n    .replaceAll('\"', \"&quot;\")\n    .replaceAll(\"'\", \"&#39;\");\n}\n\nfunction declension(value, forms) {\n  const absolute = Math.abs(value) % 100;\n  const tail = absolute % 10;\n  if (absolute > 10 && absolute < 20) {\n    return forms[2];\n  }\n  if (tail > 1 && tail < 5) {\n    return forms[1];\n  }\n  if (tail === 1) {\n    return forms[0];\n  }\n  return forms[2];\n}\n