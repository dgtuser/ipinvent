ass=\"action-square delete\" type=\"button\" data-delete-record=\"${record.id}\" data-ip=\"${escapeHtml(record.ipAddress)}\">X</button>\n                  </div>\n                `\n                : '<span class=\"subtle-text\">read-only</span>'\n            }\n          </td>\n        </tr>\n      `;\n    })\n    .join(\"\");\n}\n\nfunction renderGroups() {\n  if (!state.groups.length) {\n    elements.groupList.innerHTML = `\n      <div class=\"empty-state\">\n        <p>\u0413\u0440\u0443\u043f\u043f\u044b \u043f\u043e\u043a\u0430 \u043d\u0435 \u0441\u043e\u0437\u0434\u0430\u043d\u044b.</p>\n      </div>\n    `;\n    return;\n  }\n\n  const editor = canEditInventory();\n  elements.groupList.innerHTML = state.groups\n    .map((group) => {\n      const recordCount = state.records.filter((record) => record.groupId === group.id).length;\n      return `\n        <article class=\"group-card\">\n          <div class=\"group-top\">\n            <div>\n              <h4>${escapeHtml(group.name)}</h4>\n              <p>${escapeHtml(group.description || \"\u0411\u0435\u0437 \u043e\u043f\u0438\u0441\u0430\u043d\u0438\u044f\")}</p>\n            </div>\n            <div class=\"badge-row\">\n              <span class=\"group-badge\">${recordCount} ${declension(recordCount, [\n                \"\u0437\u0430\u043f\u0438\u0441\u044c\",\n                \"\u0437\u0430\u043f\u0438\u0441\u0438\",\n                \"\u0437\u0430\u043f\u0438\u0441\u0435\u0439\",\n              ])}</span>\n              <button class=\"button button-ghost\" type=\"button\" data-filter-group=\"${escapeHtml(group.name)}\">\n                \u0424\u0438\u043b\u044c\u0442\u0440\n              </button>\n              ${\n                editor\n                  ? `<button class=\"button button-ghost\" type=\"button\" data-delete-group=\"${group.id}\" data-group-name=\"${escapeHtml(group.name)}\">\u0423\u0434\u0430\u043b\u0438\u0442\u044c</button>`\n                  : \"\"\n              }\n            </div>\n          </div>\n        </article>\n      `;\n    })\n    .join(\"\");\n}\n\nfunction renderUsers() {\n  if (!isAdmin()) {\n    elements.usersTableBody.innerHTML = `\n      <tr>\n        <td colspan=\"6\">\n          <div class=\"empty-state\"><p>\u041f\u0440\u043e\u0441\u043c\u043e\u0442\u0440 \u043f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u0435\u0439 \u0434\u043e\u0441\u0442\u0443\u043f\u0435\u043d \u0442\u043e\u043b\u044c\u043a\u043e \u0430\u0434\u043c\u0438\u043d\u0438\u0441\u0442\u0440\u0430\u0442\u043e\u0440\u0430\u043c.</p></div>\n        </td>\n      </tr>\n    `;\n    return;\n  }\n\n  if (!state.users.length) {\n    elements.usersTableBody.innerHTML = `\n      <tr>\n        <td colspan=\"6\">\n          <div class=\"empty-state\"><p>\u041f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u0438 \u043f\u043e\u043a\u0430 \u043d\u0435 \u0441\u043e\u0437\u0434\u0430\u043d\u044b.</p></div>\n        </td>\n      </tr>\n    `;\n    return;\n  }\n\n  elements.usersTableBody.innerHTML = state.users\n    .map((user) => {\n      return `\n        <tr>\n          <td>${escapeHtml(user.username)}</td>\n          <td>${escapeHtml(user.displayName || \"\u2014\")}</td>\n          <td><span class=\"role-badge ${escapeHtml(user.role)}\">${escapeHtml(roleLabel(user.role))}</span></td>\n          <td>${user.isActive ? '<span class=\"audit-badge ok\">\u0410\u043a\u0442\u0438\u0432\u0435\u043d</span>' : '<span class=\"audit-badge warning\">\u041e\u0442\u043a\u043b\u044e\u0447\u0435\u043d</span>'}</td>\n          <td class=\"subtle-text\">${escapeHtml(formatDateTime(user.lastLoginAt) || \"\u2014\")}</td>\n          <td>\n            <div class=\"action-buttons\">\n              <button class=\"action-square edit\" type=\"button\" data-edit-user=\"${user.id}\">E</button>\n            </div>\n          </td>\n        </tr>\n      `;\n    })\n    .join(\"\");\n}\n\nfunction renderActivity() {\n  if (!state.activity.length) {\n    elements.activityList.innerHTML = `\n      <div class=\"empty-state\">\n        <p>\u0418\u0441\u0442\u043e\u0440\u0438\u044f \u0434\u0435\u0439\u0441\u0442\u0432\u0438\u0439 \u043f\u043e\u043a\u0430 \u043f\u0443\u0441\u0442\u0430.</p>\n      </div>\n    `;\n    return;\n  }\n\n  elements.activityList.innerHTML = state.activity\n    .map((item) => {\n      return `\n        <article class=\"activity-card\">\n          <div class=\"activity-top\">\n            <div>\n              <h4>${escapeHtml(activityLabel(item.action, item.entityType))}</h4>\n              <p>${escapeHtml(item.entityLabel)}</p>\n            </div>\n            <div class=\"activity-meta\">${escapeHtml(formatDateTime(item.createdAt))}</div>\n          </div>\n          <p>${escapeHtml(item.actor)}${item.details ? ` \u00b7 ${escapeHtml(item.details)}` : \"\"}</p>\n        </article>\n      `;\n    })\n    .join(\"\");\n}\n\nfunction openRecordModal(record = null) {\n  if (!canEditInventory()) {\n    return;\n  }\n\n  state.editingRecord = record;\n  elements.recordModalTitle.textContent = record ? \"\u0420\u0435\u0434\u0430\u043a\u0442\u0438\u0440\u043e\u0432\u0430\u043d\u0438\u0435 \u0437\u0430\u043f\u0438\u0441\u0438\" : \"\u041d\u043e\u0432\u0430\u044f \u0437\u0430\u043f\u0438\u0441\u044c\";\n  elements.recordForm.reset();\n  elements.recordForm.elements.namedItem(\"id\").value = record?.id || \"\";\n\n  if (record) {\n    Object.entries(record).forEach(([key, value]) => {\n      const field = elements.recordForm.elements.namedItem(key);\n      if (field) {\n        field.value = value || \"\";\n      }\n    });\n  } else {\n    elements.recordForm.elements.namedItem(\"status\").value = \"Active\";\n    elements.recordForm.elements.namedItem(\"environment\").value = \"Production\";\n  }\n\n  elements.recordModal.classList.remove(\"hidden\");\n  elements.recordModal.setAttribute(\"aria-hidden\", \"false\");\n}\n\nfunction closeRecordModal() {\n  state.editingRecord = null;\n  elements.recordModal.classList.add(\"hidden\");\n  elements.recordModal.setAttribute(\"aria-hidden\", \"true\");\n  elements.recordForm.reset();\n}\n\nfunction openUserModal(user = null) {\n  if (!isAdmin()) {\n    return;\n  }\n\n  state.editingUser = user;\n  elements.userModalTitle.textContent = user ? \"\u0420\u0435\u0434\u0430\u043a\u0442\u0438\u0440\u043e\u0432\u0430\u043d\u0438\u0435 \u043f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u044f\" : \"\u041d\u043e\u0432\u044b\u0439 \u043f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u044c\";\n  elements.userForm.reset();\n  elements.userForm.elements.namedItem(\"id\").value = user?.id || \"\";\n  elements.userUsernameInput.readOnly = Boolean(user);\n  elements.userPasswordInput.required = !user;\n  elements.userActiveInput.checked = user ? Boolean(user.isActive) : true;\n\n  if (user) {\n    elements.userForm.elements.namedItem(\"username\").value = user.username || \"\";\n    elements.userForm.elements.namedItem(\"displayName\").value = user.displayName || \"\";\n    elements.userForm.elements.namedItem(\"role\").value = user.role || \"viewer\";\n  }\n\n  elements.userModal.classList.remove(\"hidden\");\n  elements.userModal.setAttribute(\"aria-hidden\", \"false\");\n}\n\nfunction closeUserModal() {\n  state.editingUser = null;\n  elements.userModal.classList.add(\"hidden\");\n  elements.userModal.setAttribute(\"aria-hidden\", \"true\");\n  elements.userForm.reset();\n}\n\nasync function handleLoginSubmit(event) {\n  event.preventDefault();\n  const formData = new FormData(elements.loginForm);\n  try {\n    const payload = await apiRequest(\"/api/login\", {\n      method: \"POST\",\n      body: Object.fromEntries(formData.entries()),\n      skipAuthHandling: true,\n    });\n    state.user = payload.user;\n    elements.loginForm.reset();\n    await enterApplication();\n    showToast(\"\u0412\u0445\u043e\u0434 \u0432\u044b\u043f\u043e\u043b\u043d\u0435\u043d\", \"success\");\n  } catch (error) {\n    showToast(error.