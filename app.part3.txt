);\n  renderUsers();\n  renderActivity();\n}\n\nfunction applyPermissions() {\n  const admin = isAdmin();\n  const editor = canEditInventory();\n\n  elements.adminOnly.forEach((element) => {\n    element.hidden = !admin;\n  });\n\n  elements.editorOnly.forEach((element) => {\n    element.hidden = !editor;\n  });\n\n  elements.openRecordButton.hidden = !editor;\n  elements.importButton.hidden = !editor;\n  elements.groupForm.classList.toggle(\"hidden\", !editor);\n\n  if (!admin && state.currentView === \"users\") {\n    state.currentView = \"inventory\";\n  }\n}\n\nfunction renderHeader() {\n  elements.currentUserName.textContent = state.user?.displayName || state.user?.username || \"\u2014\";\n  elements.currentUserRole.textContent = roleLabel(state.user?.role || \"viewer\");\n}\n\nfunction renderStats() {\n  const auditCount = state.records.filter((record) => needsAudit(record.lastAudit)).length;\n  elements.statTotal.textContent = String(state.records.length);\n  elements.statGroups.textContent = String(state.groups.length);\n  elements.statAudit.textContent = String(auditCount);\n  elements.statUsers.textContent = isAdmin()\n    ? String(state.users.filter((user) => user.isActive).length)\n    : \"\u2014\";\n}\n\nfunction renderViewState() {\n  const config = {\n    inventory: {\n      caption: \"IP address management\",\n      title: \"\u0420\u0435\u0435\u0441\u0442\u0440 IP-\u0430\u0434\u0440\u0435\u0441\u043e\u0432\",\n      section: elements.inventoryView,\n    },\n    groups: {\n      caption: \"Network segmentation\",\n      title: \"\u0413\u0440\u0443\u043f\u043f\u044b \u0438 \u0441\u0435\u0433\u043c\u0435\u043d\u0442\u044b\",\n      section: elements.groupsView,\n    },\n    users: {\n      caption: \"Access control\",\n      title: \"\u041f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u0438 \u0441\u0438\u0441\u0442\u0435\u043c\u044b\",\n      section: elements.usersView,\n    },\n    activity: {\n      caption: \"Audit trail\",\n      title: \"\u041b\u0435\u043d\u0442\u0430 \u0434\u0435\u0439\u0441\u0442\u0432\u0438\u0439\",\n      section: elements.activityView,\n    },\n  };\n\n  Object.entries(config).forEach(([key, view]) => {\n    view.section.classList.toggle(\"hidden\", key !== state.currentView);\n  });\n\n  elements.navButtons.forEach((button) => {\n    button.classList.toggle(\"active\", button.dataset.view === state.currentView);\n  });\n\n  const active = config[state.currentView];\n  elements.viewCaption.textContent = active.caption;\n  elements.viewTitle.textContent = active.title;\n}\n\nfunction renderFilters() {\n  fillSelect(elements.groupFilter, \"\u0412\u0441\u0435 \u0433\u0440\u0443\u043f\u043f\u044b\", state.groups.map((group) => group.name));\n  fillSelect(\n    elements.statusFilter,\n    \"\u0412\u0441\u0435 \u0441\u0442\u0430\u0442\u0443\u0441\u044b\",\n    uniqueValues(state.records.map((record) => record.status).filter(Boolean)),\n  );\n  fillSelect(\n    elements.environmentFilter,\n    \"\u0412\u0441\u0435 \u0441\u0440\u0435\u0434\u044b\",\n    uniqueValues(state.records.map((record) => record.environment).filter(Boolean)),\n  );\n\n  elements.searchInput.value = state.filters.query;\n  elements.groupFilter.value = state.filters.group;\n  elements.statusFilter.value = state.filters.status;\n  elements.environmentFilter.value = state.filters.environment;\n}\n\nfunction fillSelect(element, defaultLabel, values) {\n  const currentValue = element.value || \"all\";\n  const html = [`<option value=\"all\">${escapeHtml(defaultLabel)}</option>`]\n    .concat(\n      values.map(\n        (value) => `<option value=\"${escapeHtml(value)}\">${escapeHtml(value)}</option>`,\n      ),\n    )\n    .join(\"\");\n\n  element.innerHTML = html;\n  element.value = values.includes(currentValue) || currentValue === \"all\" ? currentValue : \"all\";\n}\n\nfunction renderGroupDatalist() {\n  elements.groupsDatalist.innerHTML = state.groups\n    .map((group) => `<option value=\"${escapeHtml(group.name)}\"></option>`)\n    .join(\"\");\n}\n\nfunction renderSortState() {\n  elements.sortButtons.forEach((button) => {\n    const active = button.dataset.sort === state.sort.key;\n    button.classList.toggle(\"active\", active);\n    const baseLabel = button.textContent.replace(/[\u25b2\u25bc]/g, \"\").trim();\n    button.textContent = active\n      ? `${baseLabel} ${state.sort.direction === \"asc\" ? \"\u25b2\" : \"\u25bc\"}`\n      : baseLabel;\n  });\n}\n\nfunction getFilteredRecords() {\n  const query = state.filters.query.trim().toLowerCase();\n  const filtered = state.records.filter((record) => {\n    const matchesGroup = state.filters.group === \"all\" || record.group === state.filters.group;\n    const matchesStatus = state.filters.status === \"all\" || record.status === state.filters.status;\n    const matchesEnvironment =\n      state.filters.environment === \"all\" || record.environment === state.filters.environment;\n\n    if (!matchesGroup || !matchesStatus || !matchesEnvironment) {\n      return false;\n    }\n\n    if (!query) {\n      return true;\n    }\n\n    const haystack = SEARCH_FIELDS.map((field) => String(record[field] || \"\").toLowerCase()).join(\" \" );\n    return haystack.includes(query);\n  });\n\n  return filtered.sort(compareRecords);\n}\n\nfunction compareRecords(left, right) {\n  const direction = state.sort.direction === \"asc\" ? 1 : -1;\n  const key = state.sort.key;\n\n  if (key === \"ipAddress\") {\n    return compareIp(left.ipAddress, right.ipAddress) * direction;\n  }\n\n  if (key === \"lastAudit\") {\n    const leftValue = left.lastAudit ? new Date(left.lastAudit).getTime() : 0;\n    const rightValue = right.lastAudit ? new Date(right.lastAudit).getTime() : 0;\n    return (leftValue - rightValue) * direction;\n  }\n\n  return (\n    String(left[key] || \"\").localeCompare(String(right[key] || \"\"), \"ru\", {\n      sensitivity: \"base\",\n    }) * direction\n  );\n}\n\nfunction renderInventoryTable() {\n  const records = getFilteredRecords();\n  elements.resultCounter.textContent = `${records.length} ${declension(records.length, [\n    \"\u0440\u0435\u0437\u0443\u043b\u044c\u0442\u0430\u0442\",\n    \"\u0440\u0435\u0437\u0443\u043b\u044c\u0442\u0430\u0442\u0430\",\n    \"\u0440\u0435\u0437\u0443\u043b\u044c\u0442\u0430\u0442\u043e\u0432\",\n  ])}`;\n\n  if (!records.length) {\n    elements.recordsTableBody.innerHTML = `\n      <tr>\n        <td colspan=\"11\">\n          <div class=\"empty-state\">\n            <p>\u041f\u043e \u0442\u0435\u043a\u0443\u0449\u0438\u043c \u0444\u0438\u043b\u044c\u0442\u0440\u0430\u043c \u0437\u0430\u043f\u0438\u0441\u0438 \u043d\u0435 \u043d\u0430\u0439\u0434\u0435\u043d\u044b.</p>\n          </div>\n        </td>\n      </tr>\n    `;\n    return;\n  }\n\n  elements.recordsTableBody.innerHTML = records\n    .map((record) => {\n      const canEdit = canEditInventory();\n      return `\n        <tr>\n          <td><span class=\"ip-link\">${escapeHtml(record.ipAddress)}</span></td>\n          <td><span class=\"group-badge\">${escapeHtml(record.group || \"Unassigned\")}</span></td>\n          <td>${renderStatusBadge(record.status)}</td>\n          <td class=\"subtle-text\">${escapeHtml(record.deviceType || \"\u2014\")}</td>\n          <td>${escapeHtml(record.hostname || \"\u2014\")}</td>\n          <td class=\"subtle-text\">${escapeHtml(record.dnsName || \"\u2014\")}</td>\n          <td class=\"subtle-text\">${escapeHtml(record.owner || \"\u2014\")}</td>\n          <td class=\"subtle-text\">${escapeHtml(record.location || \"\u2014\")}</td>\n          <td>${renderAuditBadge(record.lastAudit)}</td>\n          <td class=\"subtle-text\">${escapeHtml(record.description || \"\u2014\")}</td>\n          <td>\n            ${\n              canEdit\n                ? `\n                  <div class=\"action-buttons\">\n                    <button class=\"action-square edit\" type=\"button\" data-edit-record=\"${record.id}\">E</button>\n                    <button cl