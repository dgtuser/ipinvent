fetchall()\n        self.send_json(200, {\"records\": [row_to_record(row) for row in rows]})\n\n    def api_create_record(self, conn: sqlite3.Connection) -> None:\n        user = self.require_user(conn, (\"admin\", \"operator\"))\n        payload = self.request_json()\n        record = parse_record_payload(conn, payload)\n        timestamp = now_iso()\n\n        try:\n            cursor = conn.execute(\n                \"\"\"\n                INSERT INTO records (\n                    ip_address, ip_key, hostname, group_id, status, device_type, environment,\n                    subnet, vlan, gateway, mac_address, dns_name, location, owner,\n                    assigned_at, last_audit, tags, description, notes,\n                    created_by, updated_by, created_at, updated_at\n                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)\n                \"\"\",\n                (\n                    record[\"ip_address\"],\n                    record[\"ip_key\"],\n                    record[\"hostname\"],\n                    record[\"group_id\"],\n                    record[\"status\"],\n                    record[\"device_type\"],\n                    record[\"environment\"],\n                    record[\"subnet\"],\n                    record[\"vlan\"],\n                    record[\"gateway\"],\n                    record[\"mac_address\"],\n                    record[\"dns_name\"],\n                    record[\"location\"],\n                    record[\"owner\"],\n                    record[\"assigned_at\"],\n                    record[\"last_audit\"],\n                    record[\"tags\"],\n                    record[\"description\"],\n                    record[\"notes\"],\n                    user[\"id\"],\n                    user[\"id\"],\n                    timestamp,\n                    timestamp,\n                ),\n            )\n        except sqlite3.IntegrityError as error:\n            raise ApiError(\"\u0422\u0430\u043a\u043e\u0439 IP-\u0430\u0434\u0440\u0435\u0441 \u0443\u0436\u0435 \u0441\u0443\u0449\u0435\u0441\u0442\u0432\u0443\u0435\u0442\", 409) from error\n\n        row = conn.execute(\n            \"\"\"\n            SELECT r.*, g.name AS group_name\n            FROM records r\n            LEFT JOIN groups g ON g.id = r.group_id\n            WHERE r.id = ?\n            \"\"\",\n            (cursor.lastrowid,),\n        ).fetchone()\n        log_activity(conn, user[\"id\"], \"create\", \"record\", record[\"ip_address\"], \"\u0414\u043e\u0431\u0430\u0432\u043b\u0435\u043d\u0430 \u0437\u0430\u043f\u0438\u0441\u044c\")\n        self.send_json(201, {\"record\": row_to_record(row)})\n\n    def api_update_record(self, conn: sqlite3.Connection, record_id: int) -> None:\n        user = self.require_user(conn, (\"admin\", \"operator\"))\n        existing = conn.execute(\"SELECT * FROM records WHERE id = ?\", (record_id,)).fetchone()\n        if existing is None:\n            raise ApiError(\"\u0417\u0430\u043f\u0438\u0441\u044c \u043d\u0435 \u043d\u0430\u0439\u0434\u0435\u043d\u0430\", 404)\n\n        payload = self.request_json()\n        record = parse_record_payload(conn, payload)\n        timestamp = now_iso()\n\n        try:\n            conn.execute(\n                \"\"\"\n                UPDATE records\n                SET ip_address = ?, ip_key = ?, hostname = ?, group_id = ?, status = ?,\n                    device_type = ?, environment = ?, subnet = ?, vlan = ?, gateway = ?,\n                    mac_address = ?, dns_name = ?, location = ?, owner = ?,\n                    assigned_at = ?, last_audit = ?, tags = ?, description = ?, notes = ?,\n                    updated_by = ?, updated_at = ?\n                WHERE id = ?\n                \"\"\",\n                (\n                    record[\"ip_address\"],\n                    record[\"ip_key\"],\n                    record[\"hostname\"],\n                    record[\"group_id\"],\n                    record[\"status\"],\n                    record[\"device_type\"],\n                    record[\"environment\"],\n                    record[\"subnet\"],\n                    record[\"vlan\"],\n                    record[\"gateway\"],\n                    record[\"mac_address\"],\n                    record[\"dns_name\"],\n                    record[\"location\"],\n                    record[\"owner\"],\n                    record[\"assigned_at\"],\n                    record[\"last_audit\"],\n                    record[\"tags\"],\n                    record[\"description\"],\n                    record[\"notes\"],\n                    user[\"id\"],\n                    timestamp,\n                    record_id,\n                ),\n            )\n        except sqlite3.IntegrityError as error:\n            raise ApiError(\"\u0422\u0430\u043a\u043e\u0439 IP-\u0430\u0434\u0440\u0435\u0441 \u0443\u0436\u0435 \u0441\u0443\u0449\u0435\u0441\u0442\u0432\u0443\u0435\u0442\", 409) from error\n\n        row = conn.execute(\n            \"\"\"\n            SELECT r.*, g.name AS group_name\n            FROM records r\n            LEFT JOIN groups g ON g.id = r.group_id\n            WHERE r.id = ?\n            \"\"\",\n            (record_id,),\n        ).fetchone()\n        log_activity(conn, user[\"id\"], \"update\", \"record\", record[\"ip_address\"], \"\u0417\u0430\u043f\u0438\u0441\u044c \u043e\u0431\u043d\u043e\u0432\u043b\u0435\u043d\u0430\")\n        self.send_json(200, {\"record\": row_to_record(row)})\n\n    def api_delete_record(self, conn: sqlite3.Connection, record_id: int) -> None:\n        user = self.require_user(conn, (\"admin\", \"operator\"))\n        existing = conn.execute(\n            \"SELECT ip_address FROM records WHERE id = ?\", (record_id,)\n        ).fetchone()\n        if existing is None:\n            raise ApiError(\"\u0417\u0430\u043f\u0438\u0441\u044c \u043d\u0435 \u043d\u0430\u0439\u0434\u0435\u043d\u0430\", 404)\n\n        conn.execute(\"DELETE FROM records WHERE id = ?\", (record_id,))\n        log_activity(conn, user[\"id\"], \"delete\", \"record\", existing[\"ip_address\"], \"\u0417\u0430\u043f\u0438\u0441\u044c \u0443\u0434\u0430\u043b\u0435\u043d\u0430\")\n        self.send_json(200, {\"ok\": True})\n\n    def api_import_records(self, conn: sqlite3.Connection) -> None:\n        user = self.require_user(conn, (\"admin\", \"operator\"))\n        payload = self.request_json()\n        incoming = payload.get(\"records\")\n        if not isinstance(incoming, list):\n            raise ApiError(\"\u041e\u0436\u0438\u0434\u0430\u0435\u0442\u0441\u044f \u043c\u0430\u0441\u0441\u0438\u0432 records\", 400)\n\n        added = 0\n        updated = 0\n        skipped = 0\n        timestamp = now_iso()\n\n        for item in incoming:\n            try:\n                record = parse_record_payload(conn, item or {})\n            except ValueError:\n                skipped += 1\n                continue\n\n            existing = conn.execute(\n                \"SELECT id, created_by, created_at FROM records WHERE ip_key = ?\",\n                (record[\"ip_key\"],),\n            ).fetchone()\n            if existing is None:\n                conn.execute(\n                    \"\"\"\n                    INSERT INTO records (\n                        ip_address, ip_key, hostname, group_id, status, device_type, environment,\n                        subnet, vlan, gateway, mac_address, dns_name, location, owner,\n                        assigned_at, last_audit, tags, description, notes,\n                        created_by, updated_by, created_at, updated_at\n                    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)\n                    \"\"\",\n                    (\n                        record[\"ip_address\"],\n                        record[\"ip_key\"],\n                        record[\"hostname\"],\n                        record[\"group_id\"],\n                        record[\"s