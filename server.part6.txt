tatus\"],\n                        record[\"device_type\"],\n                        record[\"environment\"],\n                        record[\"subnet\"],\n                        record[\"vlan\"],\n                        record[\"gateway\"],\n                        record[\"mac_address\"],\n                        record[\"dns_name\"],\n                        record[\"location\"],\n                        record[\"owner\"],\n                        record[\"assigned_at\"],\n                        record[\"last_audit\"],\n                        record[\"tags\"],\n                        record[\"description\"],\n                        record[\"notes\"],\n                        user[\"id\"],\n                        user[\"id\"],\n                        timestamp,\n                        timestamp,\n                    ),\n                )\n                added += 1\n            else:\n                conn.execute(\n                    \"\"\"\n                    UPDATE records\n                    SET ip_address = ?, hostname = ?, group_id = ?, status = ?, device_type = ?,\n                        environment = ?, subnet = ?, vlan = ?, gateway = ?, mac_address = ?,\n                        dns_name = ?, location = ?, owner = ?, assigned_at = ?, last_audit = ?,\n                        tags = ?, description = ?, notes = ?, updated_by = ?, updated_at = ?\n                    WHERE id = ?\n                    \"\"\",\n                    (\n                        record[\"ip_address\"],\n                        record[\"hostname\"],\n                        record[\"group_id\"],\n                        record[\"status\"],\n                        record[\"device_type\"],\n                        record[\"environment\"],\n                        record[\"subnet\"],\n                        record[\"vlan\"],\n                        record[\"gateway\"],\n                        record[\"mac_address\"],\n                        record[\"dns_name\"],\n                        record[\"location\"],\n                        record[\"owner\"],\n                        record[\"assigned_at\"],\n                        record[\"last_audit\"],\n                        record[\"tags\"],\n                        record[\"description\"],\n                        record[\"notes\"],\n                        user[\"id\"],\n                        timestamp,\n                        existing[\"id\"],\n                    ),\n                )\n                updated += 1\n\n        log_activity(\n            conn,\n            user[\"id\"],\n            \"import\",\n            \"record\",\n            f\"CSV ({len(incoming)})\",\n            f\"\u0418\u043c\u043f\u043e\u0440\u0442: \u0434\u043e\u0431\u0430\u0432\u043b\u0435\u043d\u043e {added}, \u043e\u0431\u043d\u043e\u0432\u043b\u0435\u043d\u043e {updated}, \u043f\u0440\u043e\u043f\u0443\u0449\u0435\u043d\u043e {skipped}\",\n        )\n        self.send_json(200, {\"added\": added, \"updated\": updated, \"skipped\": skipped})\n\n    def api_list_groups(self, conn: sqlite3.Connection) -> None:\n        self.require_user(conn)\n        rows = conn.execute(\n            \"\"\"\n            SELECT g.*, COUNT(r.id) AS record_count\n            FROM groups g\n            LEFT JOIN records r ON r.group_id = g.id\n            GROUP BY g.id\n            ORDER BY lower(g.name)\n            \"\"\"\n        ).fetchall()\n        self.send_json(200, {\"groups\": [row_to_group(row) for row in rows]})\n\n    def api_create_group(self, conn: sqlite3.Connection) -> None:\n        user = self.require_user(conn, (\"admin\", \"operator\"))\n        payload = self.request_json()\n        name = normalize_text(payload.get(\"name\"))\n        description = normalize_text(payload.get(\"description\"))\n        if len(name) < 2:\n            raise ApiError(\"\u041d\u0430\u0437\u0432\u0430\u043d\u0438\u0435 \u0433\u0440\u0443\u043f\u043f\u044b \u0441\u043b\u0438\u0448\u043a\u043e\u043c \u043a\u043e\u0440\u043e\u0442\u043a\u043e\u0435\")\n        if conn.execute(\n            \"SELECT 1 FROM groups WHERE lower(name) = lower(?)\",\n            (name,),\n        ).fetchone():\n            raise ApiError(\"\u0422\u0430\u043a\u0430\u044f \u0433\u0440\u0443\u043f\u043f\u0430 \u0443\u0436\u0435 \u0441\u0443\u0449\u0435\u0441\u0442\u0432\u0443\u0435\u0442\", 409)\n\n        timestamp = now_iso()\n        cursor = conn.execute(\n            \"\"\"\n            INSERT INTO groups (name, description, created_at, updated_at)\n            VALUES (?, ?, ?, ?)\n            \"\"\",\n            (name, description, timestamp, timestamp),\n        )\n\n        row = conn.execute(\n            \"\"\"\n            SELECT g.*, 0 AS record_count\n            FROM groups g\n            WHERE g.id = ?\n            \"\"\",\n            (cursor.lastrowid,),\n        ).fetchone()\n        log_activity(conn, user[\"id\"], \"create\", \"group\", name, \"\u0414\u043e\u0431\u0430\u0432\u043b\u0435\u043d\u0430 \u0433\u0440\u0443\u043f\u043f\u0430\")\n        self.send_json(201, {\"group\": row_to_group(row)})\n\n    def api_delete_group(self, conn: sqlite3.Connection, group_id: int) -> None:\n        user = self.require_user(conn, (\"admin\", \"operator\"))\n        group = conn.execute(\"SELECT id, name FROM groups WHERE id = ?\", (group_id,)).fetchone()\n        if group is None:\n            raise ApiError(\"\u0413\u0440\u0443\u043f\u043f\u0430 \u043d\u0435 \u043d\u0430\u0439\u0434\u0435\u043d\u0430\", 404)\n\n        linked = conn.execute(\n            \"SELECT COUNT(*) AS total FROM records WHERE group_id = ?\", (group_id,)\n        ).fetchone()[\"total\"]\n        if linked:\n            raise ApiError(\"\u041d\u0435\u043b\u044c\u0437\u044f \u0443\u0434\u0430\u043b\u0438\u0442\u044c \u0433\u0440\u0443\u043f\u043f\u0443, \u043f\u043e\u043a\u0430 \u0432 \u043d\u0435\u0439 \u0435\u0441\u0442\u044c \u0437\u0430\u043f\u0438\u0441\u0438\", 409)\n\n        conn.execute(\"DELETE FROM groups WHERE id = ?\", (group_id,))\n        log_activity(conn, user[\"id\"], \"delete\", \"group\", group[\"name\"], \"\u0413\u0440\u0443\u043f\u043f\u0430 \u0443\u0434\u0430\u043b\u0435\u043d\u0430\")\n        self.send_json(200, {\"ok\": True})\n\n    def api_list_users(self, conn: sqlite3.Connection) -> None:\n        self.require_user(conn, (\"admin\",))\n        rows = conn.execute(\"SELECT * FROM users ORDER BY created_at DESC\").fetchall()\n        self.send_json(200, {\"users\": [row_to_user(row) for row in rows]})\n\n    def api_create_user(self, conn: sqlite3.Connection) -> None:\n        actor = self.require_user(conn, (\"admin\",))\n        payload = self.request_json()\n        username = normalize_text(payload.get(\"username\")).lower()\n        display_name = normalize_text(payload.get(\"displayName\")) or username\n        password = normalize_text(payload.get(\"password\"))\n        role = normalize_text(payload.get(\"role\")) or \"viewer\"\n\n        if role not in {\"admin\", \"operator\", \"viewer\"}:\n            raise ApiError(\"\u041d\u0435\u0434\u043e\u043f\u0443\u0441\u0442\u0438\u043c\u0430\u044f \u0440\u043e\u043b\u044c\")\n        if not validate_username(username):\n            raise ApiError(\"\u041b\u043e\u0433\u0438\u043d \u0434\u043e\u043b\u0436\u0435\u043d \u0441\u043e\u0434\u0435\u0440\u0436\u0430\u0442\u044c 3-32 \u0441\u0438\u043c\u0432\u043e\u043b\u0430: \u0431\u0443\u043a\u0432\u044b, \u0446\u0438\u0444\u0440\u044b, ., _, -\")\n        if len(password) < 8:\n            raise ApiError(\"\u041f\u0430\u0440\u043e\u043b\u044c \u0434\u043e\u043b\u0436\u0435\u043d \u0431\u044b\u0442\u044c \u043d\u0435 \u043a\u043e\u0440\u043e\u0447\u0435 8 \u0441\u0438\u043c\u0432\u043e\u043b\u043e\u0432\")\n\n        salt, digest = password_hash(password)\n        timestamp = now_iso()\n        try:\n            cursor = conn.execute(\n                \"\"\"\n                INSERT INTO users (\n                    username, display_name, password_salt, password_