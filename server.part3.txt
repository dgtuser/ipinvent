ess):\n        raise ValueError(\"\u0423\u043a\u0430\u0436\u0438\u0442\u0435 \u043a\u043e\u0440\u0440\u0435\u043a\u0442\u043d\u044b\u0439 IP-\u0430\u0434\u0440\u0435\u0441\")\n    if gateway and not validate_ip(gateway):\n        raise ValueError(\"\u041f\u043e\u043b\u0435 \u0448\u043b\u044e\u0437\u0430 \u0434\u043e\u043b\u0436\u043d\u043e \u0441\u043e\u0434\u0435\u0440\u0436\u0430\u0442\u044c \u043a\u043e\u0440\u0440\u0435\u043a\u0442\u043d\u044b\u0439 IP-\u0430\u0434\u0440\u0435\u0441\")\n    if not validate_mac(mac_address):\n        raise ValueError(\"MAC-\u0430\u0434\u0440\u0435\u0441 \u0443\u043a\u0430\u0437\u0430\u043d \u0432 \u043d\u0435\u0432\u0435\u0440\u043d\u043e\u043c \u0444\u043e\u0440\u043c\u0430\u0442\u0435\")\n    if not validate_date(assigned_at) or not validate_date(last_audit):\n        raise ValueError(\"\u0414\u0430\u0442\u044b \u0434\u043e\u043b\u0436\u043d\u044b \u0431\u044b\u0442\u044c \u0432 \u0444\u043e\u0440\u043c\u0430\u0442\u0435 \u0413\u0413\u0413\u0413-\u041c\u041c-\u0414\u0414\")\n\n    return {\n        \"ip_address\": ip_address,\n        \"ip_key\": ip_address.casefold(),\n        \"hostname\": normalize_text(payload.get(\"hostname\")),\n        \"group_id\": get_group_id(conn, payload.get(\"groupId\"), payload.get(\"group\")),\n        \"status\": normalize_text(payload.get(\"status\")) or \"Active\",\n        \"device_type\": normalize_text(payload.get(\"deviceType\")),\n        \"environment\": normalize_text(payload.get(\"environment\")) or \"Production\",\n        \"subnet\": normalize_text(payload.get(\"subnet\")),\n        \"vlan\": normalize_text(payload.get(\"vlan\")),\n        \"gateway\": gateway,\n        \"mac_address\": mac_address,\n        \"dns_name\": normalize_text(payload.get(\"dnsName\")),\n        \"location\": normalize_text(payload.get(\"location\")),\n        \"owner\": normalize_text(payload.get(\"owner\")),\n        \"assigned_at\": assigned_at,\n        \"last_audit\": last_audit,\n        \"tags\": normalize_tags(payload.get(\"tags\")),\n        \"description\": normalize_text(payload.get(\"description\")),\n        \"notes\": normalize_text(payload.get(\"notes\")),\n    }\n\n\ndef create_session(conn: sqlite3.Connection, user_id: int) -> tuple[str, str]:\n    raw_token = secrets.token_urlsafe(32)\n    expires_at = (now_utc() + timedelta(days=SESSION_DURATION_DAYS)).isoformat()\n    conn.execute(\n        \"\"\"\n        INSERT INTO sessions (user_id, token_hash, expires_at, created_at)\n        VALUES (?, ?, ?, ?)\n        \"\"\",\n        (user_id, token_hash(raw_token), expires_at, now_iso()),\n    )\n    return raw_token, expires_at\n\n\ndef active_admin_count(conn: sqlite3.Connection) -> int:\n    row = conn.execute(\n        \"SELECT COUNT(*) AS total FROM users WHERE role = 'admin' AND is_active = 1\"\n    ).fetchone()\n    return row[\"total\"]\n\n\nclass ApiError(Exception):\n    def __init__(self, message: str, status: int = 400) -> None:\n        super().__init__(message)\n        self.message = message\n        self.status = status\n\n\nclass AppHandler(BaseHTTPRequestHandler):\n    server_version = \"IPInvent/1.0\"\n\n    def do_GET(self) -> None:\n        self.dispatch_request()\n\n    def do_POST(self) -> None:\n        self.dispatch_request()\n\n    def do_PUT(self) -> None:\n        self.dispatch_request()\n\n    def do_PATCH(self) -> None:\n        self.dispatch_request()\n\n    def do_DELETE(self) -> None:\n        self.dispatch_request()\n\n    def log_message(self, format: str, *args) -> None:\n        return\n\n    def dispatch_request(self) -> None:\n        parsed = urlparse(self.path)\n        path = parsed.path\n        try:\n            if path.startswith(\"/api/\"):\n                self.handle_api(path)\n            else:\n                self.handle_static(path)\n        except ApiError as error:\n            self.send_json(error.status, {\"error\": error.message})\n        except Exception as error:  # pragma: no cover\n            self.send_json(500, {\"error\": f\"\u0412\u043d\u0443\u0442\u0440\u0435\u043d\u043d\u044f\u044f \u043e\u0448\u0438\u0431\u043a\u0430 \u0441\u0435\u0440\u0432\u0435\u0440\u0430: {error}\"})\n\n    def handle_api(self, path: str) -> None:\n        with get_connection() as conn:\n            conn.execute(\"DELETE FROM sessions WHERE expires_at <= ?\", (now_iso(),))\n\n            if self.command == \"GET\" and path == \"/api/bootstrap\":\n                self.api_bootstrap(conn)\n                return\n            if self.command == \"POST\" and path == \"/api/setup\":\n                self.api_setup(conn)\n                return\n            if self.command == \"POST\" and path == \"/api/login\":\n                self.api_login(conn)\n                return\n            if self.command == \"POST\" and path == \"/api/logout\":\n                self.api_logout(conn)\n                return\n            if self.command == \"GET\" and path == \"/api/me\":\n                user = self.require_user(conn)\n                self.send_json(200, {\"user\": row_to_user(user)})\n                return\n            if self.command == \"GET\" and path == \"/api/records\":\n                self.api_list_records(conn)\n                return\n            if self.command == \"POST\" and path == \"/api/records\":\n                self.api_create_record(conn)\n                return\n            if self.command == \"POST\" and path == \"/api/records/import\":\n                self.api_import_records(conn)\n                return\n            if self.command == \"GET\" and path == \"/api/groups\":\n                self.api_list_groups(conn)\n                return\n            if self.command == \"POST\" and path == \"/api/groups\":\n                self.api_create_group(conn)\n                return\n            if self.command == \"GET\" and path == \"/api/users\":\n                self.api_list_users(conn)\n                return\n            if self.command == \"POST\" and path == \"/api/users\":\n                self.api_create_user(conn)\n                return\n            if self.command == \"GET\" and path == \"/api/activity\":\n                self.api_list_activity(conn)\n                return\n\n            record_id = self.extract_id(path, \"/api/records/\")\n            if record_id is not None:\n                if self.command == \"PUT\":\n                    self.api_update_record(conn, record_id)\n                    return\n                if self.command == \"DELETE\":\n                    self.api_delete_record(conn, record_id)\n                    return\n\n            group_id = self.extract_id(path, \"/api/groups/\")\n            if group_id is not None and self.command == \"DELETE\":\n                self.api_delete_group(conn, group_id)\n                return\n\n            user_id = self.extract_id(path, \"/api/users/\")\n            if user_id is not None and self.command == \"PATCH\":\n                self.api_update_user(conn, user_id)\n                return\n\n        raise ApiError(\"\u041c\u0430\u0440\u0448\u0440\u0443\u0442 \u043d\u0435 \u043d\u0430\u0439\u0434\u0435\u043d\", 404)\n\n    def handle_static(self, path: str) -> None:\n        target = STATIC_FILES.get(path)\n        if target is None or not target.exists():\n            self.send_json(404, {\"error\": \"\u0424\u0430\u0439\u043b \u043d\u0435 \u043d\u0430\u0439\u0434\u0435\u043d\"})\n            return\n\n        content = target.read_bytes()\n        content_type, _ = mimetypes.guess_type(target.name)\n        self.send_response(200)\n        self.send_header(\"Content-Type\", content_type or \"application/octet-stream\")\n        self.send_header(\"Content-Length\", str(len(content)))\n        self.end_headers()\n        self.wfile.write(content)\n\n    def extract_id(self, path: str, prefix: str) -> int | None:\n        if not path.startswith(prefix):\n            return None\n        tail = path[len(prefix) :]\n        if not tail.isdigit():\n    