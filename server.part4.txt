        raise ApiError(\"\u041d\u0435\u043a\u043e\u0440\u0440\u0435\u043a\u0442\u043d\u044b\u0439 \u0438\u0434\u0435\u043d\u0442\u0438\u0444\u0438\u043a\u0430\u0442\u043e\u0440\", 404)\n        return int(tail)\n\n    def request_json(self) -> dict:\n        length = int(self.headers.get(\"Content-Length\", \"0\"))\n        if length <= 0:\n            return {}\n        payload = self.rfile.read(length)\n        try:\n            return json.loads(payload.decode(\"utf-8\"))\n        except json.JSONDecodeError as error:\n            raise ApiError(f\"\u041d\u0435\u043a\u043e\u0440\u0440\u0435\u043a\u0442\u043d\u044b\u0439 JSON: {error.msg}\", 400) from error\n\n    def read_session_token(self) -> str | None:\n        raw_cookie = self.headers.get(\"Cookie\")\n        if not raw_cookie:\n            return None\n        jar = cookies.SimpleCookie()\n        jar.load(raw_cookie)\n        session = jar.get(SESSION_COOKIE)\n        return session.value if session else None\n\n    def current_user(self, conn: sqlite3.Connection) -> sqlite3.Row | None:\n        token = self.read_session_token()\n        if not token:\n            return None\n\n        row = conn.execute(\n            \"\"\"\n            SELECT u.*\n            FROM sessions s\n            JOIN users u ON u.id = s.user_id\n            WHERE s.token_hash = ? AND s.expires_at > ? AND u.is_active = 1\n            \"\"\",\n            (token_hash(token), now_iso()),\n        ).fetchone()\n        return row\n\n    def require_user(\n        self, conn: sqlite3.Connection, allowed_roles: tuple[str, ...] | None = None\n    ) -> sqlite3.Row:\n        user = self.current_user(conn)\n        if user is None:\n            raise ApiError(\"\u0422\u0440\u0435\u0431\u0443\u0435\u0442\u0441\u044f \u0430\u0432\u0442\u043e\u0440\u0438\u0437\u0430\u0446\u0438\u044f\", 401)\n        if allowed_roles and user[\"role\"] not in allowed_roles:\n            raise ApiError(\"\u041d\u0435\u0434\u043e\u0441\u0442\u0430\u0442\u043e\u0447\u043d\u043e \u043f\u0440\u0430\u0432 \u0434\u043b\u044f \u044d\u0442\u043e\u0433\u043e \u0434\u0435\u0439\u0441\u0442\u0432\u0438\u044f\", 403)\n        return user\n\n    def send_json(\n        self,\n        status: int,\n        payload: dict,\n        headers: list[tuple[str, str]] | None = None,\n    ) -> None:\n        body = json_dumps(payload)\n        self.send_response(status)\n        self.send_header(\"Content-Type\", \"application/json; charset=utf-8\")\n        self.send_header(\"Content-Length\", str(len(body)))\n        if headers:\n            for key, value in headers:\n                self.send_header(key, value)\n        self.end_headers()\n        self.wfile.write(body)\n\n    def session_cookie_header(self, token: str, expires_at: str) -> tuple[str, str]:\n        expires = datetime.fromisoformat(expires_at).strftime(\"%a, %d %b %Y %H:%M:%S GMT\")\n        value = (\n            f\"{SESSION_COOKIE}={token}; HttpOnly; Path=/; SameSite=Lax; Expires={expires}\"\n        )\n        return (\"Set-Cookie\", value)\n\n    def clear_session_cookie_header(self) -> tuple[str, str]:\n        return (\n            \"Set-Cookie\",\n            f\"{SESSION_COOKIE}=; HttpOnly; Path=/; SameSite=Lax; Max-Age=0\",\n        )\n\n    def api_bootstrap(self, conn: sqlite3.Connection) -> None:\n        total_users = conn.execute(\"SELECT COUNT(*) AS total FROM users\").fetchone()[\"total\"]\n        user = self.current_user(conn)\n        self.send_json(\n            200,\n            {\n                \"setupRequired\": total_users == 0,\n                \"user\": row_to_user(user),\n            },\n        )\n\n    def api_setup(self, conn: sqlite3.Connection) -> None:\n        total_users = conn.execute(\"SELECT COUNT(*) AS total FROM users\").fetchone()[\"total\"]\n        if total_users:\n            raise ApiError(\"\u041f\u0435\u0440\u0432\u0438\u0447\u043d\u0430\u044f \u043d\u0430\u0441\u0442\u0440\u043e\u0439\u043a\u0430 \u0443\u0436\u0435 \u0432\u044b\u043f\u043e\u043b\u043d\u0435\u043d\u0430\", 409)\n\n        payload = self.request_json()\n        username = normalize_text(payload.get(\"username\")).lower()\n        display_name = normalize_text(payload.get(\"displayName\")) or username\n        password = normalize_text(payload.get(\"password\"))\n\n        if not validate_username(username):\n            raise ApiError(\"\u041b\u043e\u0433\u0438\u043d \u0434\u043e\u043b\u0436\u0435\u043d \u0441\u043e\u0434\u0435\u0440\u0436\u0430\u0442\u044c 3-32 \u0441\u0438\u043c\u0432\u043e\u043b\u0430: \u0431\u0443\u043a\u0432\u044b, \u0446\u0438\u0444\u0440\u044b, ., _, -\")\n        if len(password) < 8:\n            raise ApiError(\"\u041f\u0430\u0440\u043e\u043b\u044c \u0434\u043e\u043b\u0436\u0435\u043d \u0431\u044b\u0442\u044c \u043d\u0435 \u043a\u043e\u0440\u043e\u0447\u0435 8 \u0441\u0438\u043c\u0432\u043e\u043b\u043e\u0432\")\n\n        salt, digest = password_hash(password)\n        timestamp = now_iso()\n        cursor = conn.execute(\n            \"\"\"\n            INSERT INTO users (\n                username, display_name, password_salt, password_hash, role,\n                is_active, created_at, updated_at\n            ) VALUES (?, ?, ?, ?, 'admin', 1, ?, ?)\n            \"\"\",\n            (username, display_name, salt, digest, timestamp, timestamp),\n        )\n        user_id = cursor.lastrowid\n\n        seed_sample_records(conn, user_id)\n        log_activity(conn, user_id, \"create\", \"user\", username, \"\u041f\u0435\u0440\u0432\u044b\u0439 \u0430\u0434\u043c\u0438\u043d\u0438\u0441\u0442\u0440\u0430\u0442\u043e\u0440 \u0441\u043e\u0437\u0434\u0430\u043d\")\n\n        raw_token, expires_at = create_session(conn, user_id)\n        user = conn.execute(\"SELECT * FROM users WHERE id = ?\", (user_id,)).fetchone()\n        self.send_json(\n            201,\n            {\"user\": row_to_user(user)},\n            headers=[self.session_cookie_header(raw_token, expires_at)],\n        )\n\n    def api_login(self, conn: sqlite3.Connection) -> None:\n        payload = self.request_json()\n        username = normalize_text(payload.get(\"username\")).lower()\n        password = normalize_text(payload.get(\"password\"))\n\n        user = conn.execute(\"SELECT * FROM users WHERE username = ?\", (username,)).fetchone()\n        if user is None or not verify_password(password, user[\"password_salt\"], user[\"password_hash\"]):\n            raise ApiError(\"\u041d\u0435\u0432\u0435\u0440\u043d\u044b\u0439 \u043b\u043e\u0433\u0438\u043d \u0438\u043b\u0438 \u043f\u0430\u0440\u043e\u043b\u044c\", 401)\n        if not user[\"is_active\"]:\n            raise ApiError(\"\u041f\u043e\u043b\u044c\u0437\u043e\u0432\u0430\u0442\u0435\u043b\u044c \u0434\u0435\u0430\u043a\u0442\u0438\u0432\u0438\u0440\u043e\u0432\u0430\u043d\", 403)\n\n        conn.execute(\n            \"UPDATE users SET last_login_at = ?, updated_at = ? WHERE id = ?\",\n            (now_iso(), now_iso(), user[\"id\"]),\n        )\n        raw_token, expires_at = create_session(conn, user[\"id\"])\n        refreshed = conn.execute(\"SELECT * FROM users WHERE id = ?\", (user[\"id\"],)).fetchone()\n        self.send_json(\n            200,\n            {\"user\": row_to_user(refreshed)},\n            headers=[self.session_cookie_header(raw_token, expires_at)],\n        )\n\n    def api_logout(self, conn: sqlite3.Connection) -> None:\n        token = self.read_session_token()\n        if token:\n            conn.execute(\"DELETE FROM sessions WHERE token_hash = ?\", (token_hash(token),))\n        self.send_json(200, {\"ok\": True}, headers=[self.clear_session_cookie_header()])\n\n    def api_list_records(self, conn: sqlite3.Connection) -> None:\n        self.require_user(conn)\n        rows = conn.execute(\n            \"\"\"\n            SELECT r.*, g.name AS group_name\n            FROM records r\n            LEFT JOIN groups g ON g.id = r.group_id\n            ORDER BY r.ip_key ASC\n            \"\"\"\n        ).